name: Build Linux Binary

on:
  pull_request:
    branches: ['**']

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build binary
        run: npm run build

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: linux-binary
          path: dist/agent-pr-monitor
          retention-days: 7
